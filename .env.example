# Copy this file to .env and fill in your values.
# Never commit .env — it may contain secret API keys.

# Port the server listens on (default: 3000)
PORT=3000

# --- Replicate (required for real /generate) ---
# Create a token: https://replicate.com/account/api-tokens
# Paste the token that starts with r8_...
REPLICATE_API_TOKEN=r8_paste_your_token_here

# Optional: old name still works if REPLICATE_API_TOKEN is not set.
# AI_API_KEY=r8_paste_your_token_here

# Allowed browser origin for CORS (your Framer-published site).
# Examples:
#   https://your-project.framer.website
#   https://your-custom-domain.com
# For local testing from any tool, you can leave CORS_ORIGIN unset (permissive CORS in this starter).
CORS_ORIGIN=https://your-site.framer.website
